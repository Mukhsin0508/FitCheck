{
  "name": "@fitcheck/affiliates",
  "version": "0.1.0",
  "private": true,
  "description": "Affiliate layer: normalized product schema, per-network deep links (Awin, Rakuten, CJ, Partnerize, direct), subid attribution, postback parsing.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
